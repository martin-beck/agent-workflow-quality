# End-to-end discussion TUI integration contract

The `discussion-tui-integration` profile checks one bounded offline trace across
agent/user session intake, synchronized panes, candidate and custom proposal
evaluation, persistence, reconciliation, and non-authorizing handoff mapping.
It proves only public record shape and cross-stage identity; UI behavior, user
intent, provider execution, and consumer-native gates remain separate evidence.
