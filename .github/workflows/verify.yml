name: Verify

"on":
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d
        with:
          version: "0.12.8"
          enable-cache: true
          cache-dependency-glob: uv.lock
      - name: Install locked quality environment
        run: uv sync --locked --only-group quality
      - name: Install checksum-pinned shell quality tools
        run: |
          uv run python scripts/install_shell_tools.py --prefix "${RUNNER_TEMP}/awq-shell-tools"
          echo "${RUNNER_TEMP}/awq-shell-tools/bin" >> "${GITHUB_PATH}"
      - name: Install checksum-pinned documentation quality tools
        run: |
          uv run python scripts/install_documentation_tools.py --prefix "${RUNNER_TEMP}/awq-doc-tools"
          echo "${RUNNER_TEMP}/awq-doc-tools/bin" >> "${GITHUB_PATH}"
      - name: Install checksum-pinned schema quality tools
        run: |
          uv run python scripts/install_schema_tools.py --prefix "${RUNNER_TEMP}/awq-schema-tools"
          echo "${RUNNER_TEMP}/awq-schema-tools/bin" >> "${GITHUB_PATH}"
      - name: Install checksum-pinned Rust stable toolchain
        run: |
          uv run python scripts/install_rust_tools.py --prefix "${RUNNER_TEMP}/awq-rust-tools"
          echo "${RUNNER_TEMP}/awq-rust-tools/bin" >> "${GITHUB_PATH}"
      - name: Install checksum-pinned Rust supply tools
        run: |
          uv run python scripts/install_rust_supply_tools.py \
            --prefix "${RUNNER_TEMP}/awq-rust-supply-tools" \
            --rust-tools-prefix "${RUNNER_TEMP}/awq-rust-tools"
          echo "${RUNNER_TEMP}/awq-rust-supply-tools/bin" >> "${GITHUB_PATH}"
      - name: Install checksum-pinned advanced Rust tools
        run: |
          uv run python scripts/install_rust_advanced_tools.py \
            --prefix "${RUNNER_TEMP}/awq-rust-advanced-tools"
          toolchain="${RUNNER_TEMP}/awq-rust-advanced-tools/rustup/toolchains/nightly-2026-09-01-x86_64-unknown-linux-gnu/bin"
          env -i \
            PATH=/usr/bin:/bin \
            LANG=C.UTF-8 \
            LC_ALL=C.UTF-8 \
            TMPDIR="${RUNNER_TEMP}" \
            CARGO_HOME="${RUNNER_TEMP}/awq-rust-advanced-tools/runtime-cargo" \
            CARGO_NET_OFFLINE=false \
            RUSTC="${toolchain}/rustc" \
            RUSTDOC="${toolchain}/rustdoc" \
            "${toolchain}/cargo" \
            fetch --locked \
            --manifest-path fixtures/conforming/rust-advanced/fuzz/Cargo.toml
          echo "${RUNNER_TEMP}/awq-rust-advanced-tools/bin" >> "${GITHUB_PATH}"
      - name: Source license headers
        run: uv run python scripts/check_source_headers.py
      - name: Format lint and type
        run: |
          uv run ruff format --check src tests scripts
          uv run ruff check src tests scripts
          uv run mypy src tests scripts
      - name: Policy weakening gate
        if: github.event_name == 'pull_request'
        env:
          BASE: ${{ github.event.pull_request.base.sha }}
          HEAD: ${{ github.event.pull_request.head.sha }}
        run: uv run awq --root . policy-diff "$BASE" "$HEAD" --format json
      - name: Tests and branch coverage
        run: |
          uv run coverage run --branch -m unittest discover -s tests -p 'test_*.py'
          uv run coverage report --fail-under=95
      - name: Contracts generation and self-check
        run: |
          uv run python scripts/generate_catalog.py --check
          uv run python scripts/validate_contracts.py
          uv run awq --root . doctor --format json
          uv run awq --root . check --tier pr --format json
      - name: Build and install wheel
        run: |
          uv build
          uv run python scripts/verify_distribution.py dist/*
          uv venv "${RUNNER_TEMP}/awq-smoke"
          uv pip install --python "${RUNNER_TEMP}/awq-smoke/bin/python" dist/*.whl
          "${RUNNER_TEMP}/awq-smoke/bin/awq" --root . inspect --format json
      - name: DCO certification
        if: github.event_name == 'pull_request'
        env:
          BASE: ${{ github.event.pull_request.base.sha }}
          HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          while IFS= read -r revision; do
            expected=$(git show -s --format='Signed-off-by: %an <%ae>' "$revision")
            git show -s --format=%B "$revision" | grep -Fx -- "$expected"
          done < <(git rev-list "$BASE..$HEAD")
      - name: SSH commit signature policy
        if: github.event_name == 'pull_request'
        env:
          BASE: ${{ github.event.pull_request.base.sha }}
          HEAD: ${{ github.event.pull_request.head.sha }}
        run: |
          git config gpg.ssh.allowedSignersFile "$PWD/config/allowed_signers"
          while IFS= read -r revision; do
            git verify-commit "$revision"
          done < <(git rev-list "$BASE..$HEAD")
      - name: Deterministic clean tree
        run: git diff --exit-code
